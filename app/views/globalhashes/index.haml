%h2 Recent and upcoming globalhash locations

%p
  A globalhash is the geohashing algorithm applied to the whole world. If the globalhash is near you, it is a very momentous occasion and you simply must go to it!

%p
  See
  %a{:href => 'http://geohashing.org/Globalhash'}http://geohashing.org/Globalhash
  for more information.

- @globalhashes.each do |globalhash|
  %hr/
  %h3= friendly_date(globalhash.date)
  .span-24
    .span-9.first
      = image_tag(globalhash.google_map(request, 0), :alt => "Globalhash on #{friendly_date(globalhash.date)} in #{globalhash.place_name_display}", :size => "300x200", :class => 'google_map')
      %p
        = globalhash.latitude_longitude_display
        %br/
        = globalhash.place_name_display

    .span-15.last
      
      %p.globalhash_link
        = image_tag('google_maps.png', :size => '60x60', :alt => 'Google maps')
        = link_to('google maps', globalhash.google_link)

      %p.globalhash_link
        = image_tag('bing.png', :size => '60x60', :alt => 'Bing')
        = link_to('bing maps', globalhash.bing_link)

      %p.globalhash_link
        = image_tag('osm_logo_60.png', :size => '60x60', :alt => 'Open street map')
        = link_to('open street map', globalhash.osm_link)


